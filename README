# lib-template

This repository is a template library for creating jinko libraries yourself. Ideally, it
should contain a CI, running a proper `jinko` github action and running the tests present
in the library. It should also contain a basic code example to get you started on writing
your library.
