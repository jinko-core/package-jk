# package-jk

[jinko](https://github.com/jinko-core/jinko)'s package manager library.

This library is responsible for declaring jinko packages, fetching dependencies
and executing build scripts.

Ideally, it will be included by default when running `jinko --build`.
